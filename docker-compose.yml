volumes:
  sysmonitor_logs:

services:
  sysmonitor:
    build: 
      context: .
    image: linux-sysmonitor:v2
    volumes:
      - sysmonitor_logs:/app/logs
    environment:
      - LOG_LEVEL: INFO
      - CHECK_INTERVAL: 60
    restart: unless-stopped
